# CLAUDE.md

Use os subagentes em `.claude/agents` e os skills em `.claude/skills`.
Preserve os componentes upstream em `components/`; integrações novas devem ficar em `integrations/` ou `orchestrator/`.
